# Copy this file to .env and fill in real values if using MySQL.
# If DATABASE_URL is not set, the app falls back to a local SQLite file (tasks.db).

# MySQL example:
 #DATABASE_URL=mysql+pymysql://root:yugesh@03@localhost:3306/task_manager

#DATABASE_URL=sqlite:///tasks.db
